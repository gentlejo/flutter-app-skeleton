// Default entry point — delegates to dev environment.
// Use `flutter run -t lib/main_dev.dart` or `flutter run -t lib/main_prod.dart`
// for explicit environment selection.
import 'main_dev.dart' as dev;

void main() {
  dev.main();
}
